name: New Documentation Folder Configuration
description: >-
  Configure a new gitignored documentation folder and rclone remote sync
  (GDrive first) — requires prior Required Tools SA JSON, agent configures
  rclone with service_account_file, dry-run exit, then full pull.
commands:
  - phrase: configure new documentation folder
    description: >-
      Walk through local folder + gitignore, GDrive remote link, agent rclone
      remote via SA JSON (exits with dry-run), then full pull sync.
