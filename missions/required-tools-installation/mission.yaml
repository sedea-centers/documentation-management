name: Required Tools Installation
description: >-
  Install and verify tools required by the Documentation Management Center —
  shell probes, install consent, OS-specific install methods, and documented
  dry-runs.
commands:
  - phrase: install required tools
    description: >-
      Run the required-tools install and verify protocol for Documentation
      Management (probe, consent, install, dry-run).
